#!/bin/sh -e

cd <%= @path %>

case $1 in
  start)
    su -c '<%= @path %>/bin/agent.sh start' <%= @user %>
  ;;
  stop)
    su -c '<%= @path %>/bin/agent.sh stop' <%= @user %>
  ;;
  restart)
    $0 stop && sleep 3 && $0 start
  ;;
esac

exit 0
